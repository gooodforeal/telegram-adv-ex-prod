# 🤖 Telegram бот для продвижения канала

Современный Telegram бот для продвижения канала с встроенной реферальной системой и автоматической раздачей игровых аккаунтов.

## 📋 Описание

Бот предназначен для эффективного продвижения Telegram-канала через реферальную систему и предоставление пользователям игровых аккаунтов в обмен на подписку. Система автоматически контролирует раздачу аккаунтов, проверяет подписку пользователей и предоставляет удобную админ-панель для управления.

## ✨ Основные возможности

### Для пользователей:
- 🎮 **Получение игровых аккаунтов** — пользователи могут получать игровые аккаунты каждые 3 недели
- 🔗 **Реферальная система** — возможность получить персональную реферальную ссылку
- ✅ **Автоматическая проверка подписки** — бот проверяет подписку на канал перед выдачей аккаунтов
- 📖 **Гайды и инструкции** — встроенная система помощи

### Для администраторов:
- 👥 **Управление пользователями** — просмотр списка всех пользователей бота
- 🎯 **Управление аккаунтами** — добавление, удаление и просмотр игровых аккаунтов
- 📊 **Админ-панель** — удобный интерфейс для администрирования
- 📝 **Логирование** — подробное логирование всех операций

## 🛠 Технологии

- **Python 3.9** — основной язык программирования
- **aiogram 3.15.0** — асинхронный фреймворк для Telegram ботов
- **SQLAlchemy 2.0.36** — ORM для работы с базой данных
- **PostgreSQL 17.0** — реляционная база данных
- **Alembic 1.14.0** — миграции базы данных
- **asyncpg 0.30.0** — асинхронный драйвер PostgreSQL
- **Docker & Docker Compose** — контейнеризация приложения
- **Pydantic 2.9.2** — валидация данных и настроек

## 📁 Структура проекта

```
telegram-ad-bot/
├── src/
│   ├── handlers/           # Обработчики команд и сообщений
│   │   ├── admin_handler.py       # Админ-функционал
│   │   ├── referral_handler.py    # Реферальная система
│   │   ├── startup_handler.py     # Основная логика выдачи аккаунтов
│   │   └── guide_handler.py       # Гайды и инструкции
│   ├── keyboards/          # Клавиатуры бота
│   │   ├── admin_kb.py           # Админ клавиатуры
│   │   └── sub_kb.py             # Клавиатуры для подписки
│   ├── messages/           # Текстовые сообщения
│   │   ├── admin_messages.py
│   │   ├── startup_messages.py
│   │   ├── refferal_messages.py
│   │   └── guide_messages.py
│   ├── middlewares/        # Middleware компоненты
│   │   ├── check_sub.py          # Проверка подписки
│   │   └── register_user.py      # Регистрация пользователей
│   ├── models/             # Модели базы данных
│   │   └── models.py
│   ├── repositories/       # Репозитории для работы с БД
│   │   ├── users_repository.py
│   │   └── accounts_repository.py
│   ├── migrations/         # Миграции Alembic
│   ├── logs/              # Конфигурация логирования
│   ├── utils/             # Вспомогательные функции
│   ├── config.py          # Конфигурация приложения
│   ├── database.py        # Настройка базы данных
│   └── main.py            # Точка входа
├── docker-compose.yaml    # Docker Compose конфигурация
├── Dockerfile            # Образ Docker
├── requirements.txt      # Зависимости Python
├── alembic.ini          # Конфигурация Alembic
└── README.md            # Документация
```

## 🚀 Установка и запуск

### Предварительные требования

- Docker и Docker Compose
- Python 3.9+ (для локальной разработки)
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Быстрый старт с Docker

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd telegram-ad-bot
```

2. **Создайте файл `.env` в корне проекта:**
```env
# PostgreSQL
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=telegram_bot
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_strong_password

# Telegram Bot
BOT_TOKEN=your_bot_token_from_botfather
ADMIN_ID=your_telegram_user_id
CHANNEL_LINK=https://t.me/your_channel
CHANNEL_ID=@your_channel_id
```

3. **Запустите приложение:**
```bash
docker-compose up -d
```

4. **Примените миграции базы данных:**
```bash
docker-compose exec bot alembic upgrade head
```

### Локальная разработка

1. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Настройте `.env` файл** (используйте localhost для PostgreSQL)

4. **Примените миграции:**
```bash
alembic upgrade head
```

5. **Запустите бота:**
```bash
python src/main.py
```

## 📝 Использование

### Команды для пользователей:

- `/start` — Начать работу с ботом и получить игровой аккаунт
- `/ref` — Получить персональную реферальную ссылку
- `/guide` — Просмотреть инструкции и гайды

### Команды для администратора:

- `/admin` — Открыть админ-панель
- **Список пользователей** — Просмотр всех пользователей бота
- **Список аккаунтов** — Просмотр всех аккаунтов в системе
- **Добавить аккаунт** — Добавление нового игрового аккаунта
- **Удалить аккаунт** — Удаление аккаунта из системы

### Работа с аккаунтами:

**Формат добавления аккаунта:**
```
Добавить аккаунт
username:password:games
username2:password2:games2
```

**Формат удаления аккаунта:**
```
Удалить аккаунт
username
```

## 🗄 База данных

### Модель данных

**Таблица Users:**
- `id` — уникальный идентификатор
- `tg_id` — Telegram ID пользователя
- `username` — имя пользователя
- `last_time_given` — время последней выдачи аккаунта
- `is_reffed` — пришел ли по реферальной ссылке
- `is_possible` — возможность получения аккаунта

**Таблица Accounts:**
- `id` — уникальный идентификатор
- `username` — имя пользователя аккаунта
- `password` — пароль
- `games` — список игр

**Таблица Users_Accounts:**
- Связующая таблица many-to-many между пользователями и аккаунтами

### Миграции

Создание новой миграции:
```bash
alembic revision --autogenerate -m "description"
```

Применение миграций:
```bash
alembic upgrade head
```

Откат миграции:
```bash
alembic downgrade -1
```

## 🔧 Конфигурация

Все настройки хранятся в файле `.env` и загружаются через Pydantic Settings:

| Параметр | Описание | Пример |
|----------|----------|--------|
| `POSTGRES_HOST` | Хост PostgreSQL | `db` или `localhost` |
| `POSTGRES_PORT` | Порт PostgreSQL | `5432` |
| `POSTGRES_DB` | Имя базы данных | `telegram_bot` |
| `POSTGRES_USER` | Пользователь БД | `postgres` |
| `POSTGRES_PASSWORD` | Пароль БД | `your_password` |
| `BOT_TOKEN` | Токен Telegram бота | `123456:ABC-DEF...` |
| `ADMIN_ID` | Telegram ID администратора | `123456789` |
| `CHANNEL_LINK` | Ссылка на канал | `https://t.me/channel` |
| `CHANNEL_ID` | ID канала | `@channel_id` |

## 📊 Логирование

Бот использует стандартный модуль `logging` Python с настроенной конфигурацией в `src/logs/config.py`. Логи включают:

- Запросы пользователей
- Административные действия
- Ошибки и исключения
- Системные события

## 🔒 Безопасность

- ✅ Все конфиденциальные данные хранятся в `.env` файле
- ✅ Административные функции доступны только по Telegram ID
- ✅ Пароли аккаунтов хранятся в защищенной базе данных
- ✅ Проверка подписки перед выдачей аккаунтов
- ✅ Rate limiting через временные ограничения (3 недели между выдачами)

## 🤝 Разработка

### Добавление нового функционала

1. Создайте новый handler в `src/handlers/`
2. Добавьте необходимые сообщения в `src/messages/`
3. При необходимости создайте клавиатуры в `src/keyboards/`
4. Зарегистрируйте router в `src/main.py`

### Изменение схемы БД

1. Измените модели в `src/models/models.py`
2. Создайте миграцию: `alembic revision --autogenerate -m "description"`
3. Проверьте созданную миграцию в `src/migrations/versions/`
4. Примените миграцию: `alembic upgrade head`

## 📈 Особенности реализации

- **Асинхронная архитектура** — все операции выполняются асинхронно для максимальной производительности
- **Middleware система** — автоматическая регистрация пользователей и проверка подписки
- **Repository pattern** — чистая архитектура для работы с базой данных
- **Deep linking** — реферальная система через encoded start links
- **Умная выдача аккаунтов** — алгоритм предотвращает повторную выдачу одних и тех же аккаунтов

## 🐛 Известные ограничения

- Раздача аккаунтов ограничена одним разом в 3 недели на пользователя
- Требуется подписка на канал для получения аккаунтов
- Администратор идентифицируется только по Telegram ID

## 📄 Лицензия

Этот проект создан для образовательных целей.

## 👨‍💻 Автор

Разработано с использованием современного стека Python и лучших практик асинхронного программирования.

---

**Приятного использования! 🚀**

Если у вас есть вопросы или предложения, пожалуйста, создайте Issue в репозитории.
